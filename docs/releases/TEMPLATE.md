# Nomad Launcher X.Y.Z — Optional name

One or two sentences on what this release is about, for someone deciding whether to update.

## What's new

- **Feature name:** what a player can now do, in plain words.

## Fixed

- What was wrong, and what happens now.

## Known issues

- Anything still broken or limited that a player could run into.

## Install and compatibility

Requires Android 8.0 or later. Same package ID (`com.rawal.pocketdeck`) and release
signing key as earlier releases, so it installs as an update. The APK, SHA256SUMS and
signing certificate metadata are attached to this release.

## Validation

- Automated: which test suites ran and passed.
- On hardware: which device, Android version and emulators were used, and what was checked.
- Not yet verified: what still needs a real device or a tester.
